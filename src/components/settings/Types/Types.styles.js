import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginBottom: 10,
  },
  title: {
    fontSize: 15,
    marginBottom: 10,
    marginLeft: 10,
    fontWeight: "bold",
  },
});
