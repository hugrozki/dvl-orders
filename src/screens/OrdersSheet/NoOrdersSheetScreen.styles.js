import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  view: {
    display: "flex",
    justifyContent: "center",
    height: "100%",
  },
  viewText: {
    padding: 10,
  },
  text: {
    fontSize: 20,
    textAlign: "center",
    marginBottom: 10,
    fontWeight: "bold",
  },
});
